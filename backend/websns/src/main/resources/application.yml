spring:
    datasource:
        url: jdbc:mysql://i6c106.p.ssafy.io:3306/websns?serverTimezone=UTC&characterEncoding=UTF-8
        # url: jdbc:mysql://localhost:3306/websns?serverTimezone=UTC&characterEncoding=UTF-8
        username: ssafy
        password: ssafy
        driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
        hibernate:
            ddl-auto: update
    properties:
        hibernate:
            # show_sql: true
            format_sql: true

logging.level:
    org.hibernate.SQL: debug
# org.hibernate.type: trace
