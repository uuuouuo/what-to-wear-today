import styled from '@emotion/styled';

const FooterNavbar = styled.div`
  position: fixed;
  bottom: 0;
  width: 100%;
  height: 5vh;
`;

const style = {
  p: 0,
};

export default { FooterNavbar, style };
