import styled from '@emotion/styled';

const Input = styled.input`
  letter-spacing: 1px;
  border: none;
  outline: none;
  box-sizing: border-box;
  padding: 1.5vh 0.7vh;
  font-size: 1.5rem;
`;

export default { Input };
