import React from 'react';
import { NextPage } from 'next';
import SetRegionTemplate from '@/template/setregion';

const SetRegion: NextPage = () => {
  return <SetRegionTemplate></SetRegionTemplate>;
};

export default SetRegion;
