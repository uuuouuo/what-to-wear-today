import React from 'react';
import { NextPage } from 'next';
import LikeFeedTemplate from '@/template/likefeed';

const LikeFeed: NextPage = () => {
  return <LikeFeedTemplate></LikeFeedTemplate>;
};

export default LikeFeed;
